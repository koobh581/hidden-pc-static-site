# hidden-pc-v1 static connected

이 패키지는 `hidden-pc-v1.zip`을 기준으로 재구성했습니다.
기존 React/Vite 구조를 유지하되, Render Static Site에 바로 올릴 수 있도록 `frontend/` 폴더 아래로 정리했고,
문의 폼은 기존 백엔드 API(`https://hiddenpc.onrender.com/api/contact`)에 연결됩니다.
